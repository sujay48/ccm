package com.cognizant.ccm.controller;

import java.util.ArrayList;
import java.util.List;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RestController;

import com.cognizant.ccm.model.GatewaySettings;

@RestController
public class GatewaySettingsController {
	
	@GetMapping
	public List<GatewaySettings> getGatewaySettingsList() {
		return new ArrayList<>();
	}
	
	@GetMapping("/{id}")
	public GatewaySettings getGatewaySettings(@PathVariable int id) {
		return null;
	}
}
